{
  "hash": "2e1fb58b01c95051f8d653ede441b995",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: manager.features.select_top_poly_features\n---\n\n\n\n```python\nmanager.features.select_top_poly_features(\n    poly_features,\n    y,\n    max_poly_features=10,\n    random_state=123,\n)\n```\n\nRank polynomial interaction columns by mutual information, keep the top K.\n\nPolynomial expansion (`create_interaction_features` with ``degree >= 2``)\ncan emit hundreds or thousands of ``poly_*`` columns. This helper caps that\nset: it scores each candidate column by its mutual information with the\ntarget and returns the names of the ``max_poly_features`` highest-scoring\ncolumns. Mutual information is estimated with `mutual_info_regression`,\nseeded by *random_state* so the selection is reproducible.\n\n## Parameters {.doc-section .doc-section-parameters}\n\n| Name              | Type                                           | Description                                                                                                                                                 | Default    |\n|-------------------|------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------|------------|\n| poly_features     | [pd](`pandas`).[DataFrame](`pandas.DataFrame`) | DataFrame containing only the candidate ``poly_*`` interaction columns to rank.                                                                             | _required_ |\n| y                 | [pd](`pandas`).[Series](`pandas.Series`)       | Target series. It is inner-joined to *poly_features* on the index and rows with missing values are dropped before scoring.                                  | _required_ |\n| max_poly_features | [int](`int`)                                   | Maximum number of columns to keep. When this is ``<= 0`` or the candidate count does not exceed it, all columns are returned unchanged. Defaults to ``10``. | `10`       |\n| random_state      | [int](`int`)                                   | Seed forwarded to `mutual_info_regression` for a deterministic estimate. Defaults to ``123``.                                                               | `123`      |\n\n## Returns {.doc-section .doc-section-returns}\n\n| Name   | Type                                  | Description                                                              |\n|--------|---------------------------------------|--------------------------------------------------------------------------|\n|        | [List](`typing.List`)\\[[str](`str`)\\] | List[str]: Names of the selected ``poly_*`` columns, ordered from        |\n|        | [List](`typing.List`)\\[[str](`str`)\\] | highest to lowest mutual information. Returns every input column (in its |\n|        | [List](`typing.List`)\\[[str](`str`)\\] | original order) when no capping is required.                             |\n\n## Raises {.doc-section .doc-section-raises}\n\n| Name   | Type                       | Description                                                        |\n|--------|----------------------------|--------------------------------------------------------------------|\n|        | [ValueError](`ValueError`) | If *poly_features* and *y* share no overlapping, non-missing rows. |\n\n## Examples {.doc-section .doc-section-examples}\n\n\n::: {#476110ba .cell execution_count=1}\n``` {.python .cell-code}\nimport numpy as np\nimport pandas as pd\nfrom spotforecast2_safe.manager.features import select_top_poly_features\n\nrng = np.random.default_rng(0)\nidx = pd.date_range(\"2024-01-01\", periods=500, freq=\"h\", tz=\"UTC\")\nsignal = rng.normal(0, 1, 500)\ny = pd.Series(signal, index=idx, name=\"target\")\npoly = pd.DataFrame(\n    {\n        \"poly_a\": signal + rng.normal(0, 0.01, 500),  # informative\n        \"poly_b\": signal * 0.5 + rng.normal(0, 0.1, 500),\n        \"poly_c\": rng.normal(0, 1, 500),  # noise\n        \"poly_d\": rng.normal(0, 1, 500),  # noise\n    },\n    index=idx,\n)\ntop = select_top_poly_features(poly, y, max_poly_features=2)\nprint(\"kept:\", top)\nassert top[0] == \"poly_a\"\nassert len(top) == 2\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nkept: ['poly_a', 'poly_b']\n```\n:::\n:::\n\n\n",
    "supporting": [
      "manager.features.select_top_poly_features_files"
    ],
    "filters": [],
    "includes": {}
  }
}