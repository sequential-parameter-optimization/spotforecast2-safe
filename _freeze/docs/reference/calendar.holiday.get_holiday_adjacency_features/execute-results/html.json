{
  "hash": "9beffc90d95962d586f24b17f10b6264",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: calendar.holiday.get_holiday_adjacency_features\n---\n\n\n\n```python\ncalendar.holiday.get_holiday_adjacency_features(\n    data,\n    start,\n    cov_end,\n    forecast_horizon,\n    tz='UTC',\n    freq='h',\n    country_code='DE',\n    state='NW',\n)\n```\n\nBuild holiday-adjacency indicators and align them to a regular time grid.\n\nGenerates ``is_brueckentag``, ``is_before_holiday``, and\n``is_after_holiday`` indicators via `create_holiday_adjacency_df()`,\nvalidates temporal coverage with `curate_holidays()`, and reindexes the\nresult to a full ``[start, cov_end]`` grid with ``fill_value=0`` so that\nnon-flagged timestamps are always zero.\n\nAll three columns are disjoint from ``is_holiday``: a public holiday itself\nnever receives a non-zero value in any of the three adjacency columns.\n\n## Parameters {.doc-section .doc-section-parameters}\n\n| Name             | Type                                                                                    | Description                                                                                                    | Default    |\n|------------------|-----------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------|------------|\n| data             | [pd](`pandas`).[DataFrame](`pandas.DataFrame`)                                          | Reference time series DataFrame used for temporal coverage validation inside `curate_holidays()`.              | _required_ |\n| start            | [Union](`typing.Union`)\\[[str](`str`), [pd](`pandas`).[Timestamp](`pandas.Timestamp`)\\] | Start timestamp.  String values are parsed with ``utc=True``.                                                  | _required_ |\n| cov_end          | [Union](`typing.Union`)\\[[str](`str`), [pd](`pandas`).[Timestamp](`pandas.Timestamp`)\\] | Inclusive end timestamp (should cover the full forecast horizon).  String values are parsed with ``utc=True``. | _required_ |\n| forecast_horizon | [int](`int`)                                                                            | Number of forecast steps ahead; passed to `curate_holidays()`.                                                 | _required_ |\n| tz               | [str](`str`)                                                                            | Timezone applied to the generated index and passed to `create_holiday_adjacency_df()`.  Defaults to ``\"UTC\"``. | `'UTC'`    |\n| freq             | [str](`str`)                                                                            | Pandas-compatible frequency string for the output index. Defaults to ``\"h\"`` (hourly).                         | `'h'`      |\n| country_code     | [str](`str`)                                                                            | ISO 3166-1 alpha-2 country code.  Defaults to ``\"DE\"`` (Germany).                                              | `'DE'`     |\n| state            | [str](`str`)                                                                            | Sub-national state/region code.  Defaults to ``\"NW\"`` (North Rhine-Westphalia).                                | `'NW'`     |\n\n## Returns {.doc-section .doc-section-returns}\n\n| Name   | Type                                           | Description                                                        |\n|--------|------------------------------------------------|--------------------------------------------------------------------|\n|        | [pd](`pandas`).[DataFrame](`pandas.DataFrame`) | pd.DataFrame: DataFrame with three integer columns                 |\n|        | [pd](`pandas`).[DataFrame](`pandas.DataFrame`) | ``is_brueckentag``, ``is_before_holiday``, ``is_after_holiday``.   |\n|        | [pd](`pandas`).[DataFrame](`pandas.DataFrame`) | The index is a tz-aware `DatetimeIndex` with the requested *freq*. |\n|        | [pd](`pandas`).[DataFrame](`pandas.DataFrame`) | All values are in ``{0, 1}``; no NaNs.                             |\n\n## Examples {.doc-section .doc-section-examples}\n\n\n::: {#93a22725 .cell execution_count=1}\n``` {.python .cell-code}\nimport pandas as pd\nfrom spotforecast2_safe.calendar import get_holiday_adjacency_features\n\n# Build a minimal synthetic reference DataFrame.\n# curate_holidays requires:\n#   adjacency_df.shape[0] == data.shape[0] + forecast_horizon.\n# With n_data=48 rows and forecast_horizon=24, we need 72 hourly\n# steps total, so cov_end = start + 71 h (inclusive date_range).\nforecast_horizon = 24\nn_data = 48\ndata = pd.DataFrame(\n    {\"load\": range(n_data)},\n    index=pd.date_range(\"2024-01-01\", periods=n_data, freq=\"h\", tz=\"UTC\"),\n)\nstart = data.index[0]\ncov_end = start + pd.Timedelta(hours=(n_data + forecast_horizon - 1))\n\nadj = get_holiday_adjacency_features(\n    data=data,\n    start=start,\n    cov_end=cov_end,\n    forecast_horizon=forecast_horizon,\n    country_code=\"DE\",\n    state=\"NW\",\n)\nprint(\"shape:\", adj.shape)\nprint(\"columns:\", adj.columns.tolist())\nassert adj.shape == (n_data + forecast_horizon, 3)\nassert list(adj.columns) == [\n    \"is_brueckentag\", \"is_before_holiday\", \"is_after_holiday\"\n]\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nshape: (72, 3)\ncolumns: ['is_brueckentag', 'is_before_holiday', 'is_after_holiday']\n```\n:::\n:::\n\n\n",
    "supporting": [
      "calendar.holiday.get_holiday_adjacency_features_files"
    ],
    "filters": [],
    "includes": {}
  }
}